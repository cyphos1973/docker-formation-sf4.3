{% extends 'base.html.twig' %}

{% block description 'An edit form for classrooms.' %}

{% block title 'Edit a classroom' %}

{% block body %}
    <section>
        <div class="container jumbotron">
            <div class="row">
                <div class="col">
                    <h1>Edit Classroom</h1>
                </div>
            </div>
            <div class="row">
                <div class="col">
                    {{ include('classroom/_form.html.twig', {'button_label': 'Update'}) }}

                    <a href="{{ path('app_classroom_index') }}" class="btn btn-sm btn-dark">back to list</a>
                    {{ include('classroom/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}