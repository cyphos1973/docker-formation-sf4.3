{% extends 'base.html.twig' %}

{% block description 'An edit form for students.' %}

{% block title 'Edit a student' %}

{% block body %}
    <section>
        <div class="container jumbotron">
            <div class="row">
                <div class="col">
                    <h1>Edit Student</h1>
                </div>
            </div>
            <div class="row">
                <div class="col">
                    {{ include('student/_form.html.twig', {'button_label': 'Update'}) }}

                    <a href="{{ path('app_student_index') }}" class="btn btn-sm btn-dark">back to list</a>
                    {{ include('student/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
